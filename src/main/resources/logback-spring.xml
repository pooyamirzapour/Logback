<?xml version="1.0" encoding="UTF-8" ?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="com.bol.logback.MaskingPatternLayout">
                    <maskPattern>\"firstName\"\s*:\s*\"(.*?)\"</maskPattern>
                    <maskPattern>\"lastName\"\s*:\s*\"(.*?)\"</maskPattern>
                    <maskPattern>\"streetName\"\s*:\s*\"(.*?)\"</maskPattern>
                    <maskPattern>\"houseNumber\"\s*:\s*\"(.*?)\"</maskPattern>
                    <maskPattern>\"postalCode\"\s*:\s*\"(.*?)\"</maskPattern>
                    <maskPattern>\"deliveryAddress\"\s*:\s*\"(.*?)\"</maskPattern>
                    <maskPattern>(\d+\.\d+\.\d+\.\d+)</maskPattern> <!-- Ip address IPv4 pattern -->
                    <maskPattern>(\w+@\w+\.\w+)</maskPattern> <!-- Email pattern -->
                    <pattern>%-5p [%d{ISO8601,UTC}] [%thread] %c: %m%n%rootException</pattern>
                </layout>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>


</configuration>